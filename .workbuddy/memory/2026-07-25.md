# 2026-07-25 工作记录

## 导出项目交接信息
- 用户要切换新窗口，需要把项目关键上下文打包带走。
- 生成 `D:\gitcangku\DB-GPT\tupu\项目交接信息导出.md`，汇总：项目定位、技术栈、目录结构、启动方式、已修复的 2 个 Bug（资产矩阵水平滚动条 / LLM 测试 [Errno 22]）、新增智谱GLM-4连接、关键 API 路径（/api/v1 前缀）、Playwright 测试环境、接手提示。
- 关键交接要点：后端必须用系统 Python 3.11 且**不带 --reload** 启动（否则 langchain/openai 调智谱报 [Errno 22]）。

## 将 tables.sql 知识整理到三个 Excel 模板（07-29）
- 用户提供了 4 个文件：tables.sql（电力配网数据模型）+ 3 个 Excel 模板（主数据/业务活动/实体关系）。
- tables.sql 含 12 张档案表(主数据) + 8 张量测表(业务活动) + 11 条外键 + 2 条拓扑关系 + 9 套参考数据码表。
- 三个 Excel 模板已有大量数据（主数据 45 实体/2257 属性，业务活动 74 实体/3283 属性，实体关系 23 条），但 SQL 的表名(cms20_*/dwd_*)与 Excel 的实体编码(dim_*)不同——Excel 用的是数仓维度表，SQL 是源系统表。
- 分析发现：概念层面大部分已覆盖（L2/L3/L4 概念已有），但属性层面有大量缺失（馈线 0/7、配变 0/8、台区 2/4 等），且 8 张量测表完全没有对应实体。
- 生成 3 个填充后文件：
  - master_data_metadata_filled.xlsx: +1 概念(调压设备) +3 实体 +50 属性
  - business_activity_metadata_filled.xlsx: +8 实体(量测表) +44 属性
  - entity_relations_filled.xlsx: +15 关系(11外键+2拓扑+2跨域)

## 重新基于正确模板填充（07-29 修正）
- 用户反馈之前用的 `business_activity_metadata (2).xlsx` 有误，改用 `(1).xlsx`。
- 新模板差异：主数据 42实体(旧46)、业务活动 76实体(旧75，多了WBS成本)、实体关系 28行(旧24)+多了「备注」列(21列)。
- 重新生成三个 filled 文件，结果：
  - master_data_metadata_filled.xlsx: +1概念(调压设备) +3实体 +50属性
  - business_activity_metadata_filled.xlsx: +8实体(量测表) +44属性
  - entity_relations_filled.xlsx: +15关系(11外键+2拓扑+2跨域)，含备注列
