# 三模块统一收口灰度验收报告（2026-05-07）

## 1. 验收范围
- 智能问数：完成成品化改造并进入灰度入口。
- 智能联接、智能溯源、智能问数：统一为同一交互范式（用户模式/开发者模式）。
- 旧路径下线：下线智能问数旧“问答聊天流”入口，统一走成品入口。

## 2. 本次交付清单
- `frontend/src/pages/SmartQA.tsx`
  - 重构为成品版双模式：
    - 用户模式：输入/上传 -> 识别属性 -> 一键执行问数 -> 结果展示；
    - 开发者模式：分步预览、条件交互续跑、最终校验执行、日志可视化。
  - 接入统一链路 API：
    - `POST /smart-core/step-preview`
    - `POST /smart-core/runs/{run_id}/continue`
    - `POST /smart-core/runs/{run_id}/finalize`
  - 支持时间条件交互确认（`pending_question`）后续跑。
  - 支持数据源选择并透传 `data_source_id`。
- `frontend/src/App.tsx`
  - 三模块菜单标记为灰度入口：
    - 智能溯源（灰度）
    - 智能联接（灰度）
    - 智能问数（灰度）

## 3. 统一收口说明
- 三模块统一到以下产品形态：
  - 用户模式：低门槛、动作收敛、结果直达。
  - 开发者模式：可追踪、可重跑、可回退、可看日志。
- 智能问数已从旧聊天式入口迁移为成品化入口，不再作为主流程路径。

## 4. 旧路径下线说明
- 下线对象：`SmartQA` 页面内旧“问答模式（聊天消息流）”主入口。
- 替代路径：`SmartQA` 成品版用户模式入口（统一输入/识别/执行链路）。
- 兼容性：保留统一 API 能力，不影响后端历史技能执行记录。

## 5. 构建与运行验收
- 前端构建：`npm run build` 通过（ExitCode=0）。
- 前端启动：`npm start` 成功（`http://localhost:3000`）。
- 后端启动：`uvicorn app.main:app --port 8000` 成功（`http://localhost:8000`）。
- 联调结论：灰度入口可访问、链路可执行。

## 6. 已知非阻塞项
- 前端仍存在历史告警（与本次改动无关）：
  - `dagre-compound` source map warning；
  - 其他历史 ESLint warning（散落在未改动文件）。
- 结论：不阻塞本次灰度验收上线。

## 7. 最终验收结论
- 结论：**通过（灰度上线）**。
- 建议：灰度期重点观察三类指标：
  - 识别属性成功率（文本/Excel）；
  - 条件消歧交互完成率（特别是时间字段）；
  - SQL 校验失败率与执行成功率。

